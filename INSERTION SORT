def Insertion_Sort(a):
	for i in range(1, len(a)):
		k =a[i]
		j = i-1
		while j >=0 and k < a[j] :
				a[j+1] = a[j]
				j -= 1
		a[j+1] = k
a = [45, 21, 19, 8, 100]
Insertion_Sort(a)
print("Result= ")	 
print(a)
